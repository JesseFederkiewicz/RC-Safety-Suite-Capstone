// esp_now_receiver.h

#ifndef _ESP_NOW_RECEIVER_h
#define _ESP_NOW_RECEIVER_h

#if defined(ARDUINO) && ARDUINO >= 100
	#include "arduino.h"
#else
	#include "WProgram.h"
#endif

void Main();

#endif

